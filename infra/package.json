{
  "name": "lldb-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK infrastructure for the lldb distributed query engine (ECS Fargate worker fleet).",
  "bin": {
    "lldb-infra": "bin/lldb.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "diff": "cdk diff"
  },
  "devDependencies": {
    "@types/jest": "^29",
    "@types/node": "^22",
    "aws-cdk": "^2",
    "jest": "^29",
    "ts-jest": "^29",
    "ts-node": "^10",
    "typescript": "~5.6"
  },
  "dependencies": {
    "aws-cdk-lib": "^2",
    "constructs": "^10"
  }
}
