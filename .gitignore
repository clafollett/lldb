# Rust build artifacts
/target
**/target

# Note: Cargo.lock IS committed — this workspace produces binaries (coordinator, worker),
# and a locked dependency graph keeps every machine on the verified version set.

# Generated TPC-H data and the local Iceberg warehouse (large, reproducible via tpchgen-cli)
/data/
*.parquet
*.tbl

# Local Iceberg SQL catalog (SQLite)
*.db
*.sqlite
*.sqlite3

# Profiling / benchmarking artifacts
flamegraph.svg
perf.data
perf.data.old
profile.json
/target/criterion/

# Editor / OS cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/*
!.vscode/extensions.json

# Throwaway git worktrees created for isolated agent runs. Transient scaffolding, not project
# config — scoped to `worktrees/` rather than all of `.claude/` so anything a repo legitimately
# commits there (settings, agent definitions) stays visible.
.claude/worktrees/
